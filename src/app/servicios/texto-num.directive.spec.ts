import { TextoNumDirective } from './texto-num.directive';

describe('TextoNumDirective', () => {
  it('should create an instance', () => {
    // @ts-ignore
    const directive = new TextoNumDirective();
    expect(directive).toBeTruthy();
  });
});
