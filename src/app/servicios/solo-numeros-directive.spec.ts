import { SoloNumerosDirective } from './solo-numeros-directive';

describe('SoloNumerosDirective', () => {
  it('should create an instance', () => {
    // @ts-ignore
    expect(new SoloNumerosDirective()).toBeTruthy();
  });
});
