import { SoloTextoDirective } from './solo-texto.directive';

describe('SoloTextoDirective', () => {
  it('should create an instance', () => {
    // @ts-ignore
    const directive = new SoloTextoDirective();
    expect(directive).toBeTruthy();
  });
});
