import { DireccionesHistSujeto } from './direcciones-hist-sujeto';

describe('DireccionesHistSujeto', () => {
  it('should create an instance', () => {
    expect(new DireccionesHistSujeto()).toBeTruthy();
  });
});
